@model PagedList.IPagedList<DiamDev.Give.Entities.Recibo>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Venta x Recibo Listado";
}

<div class="row">
    <div class="col-md-12">
        <form action="" method="get" class="form-inline">
            <div class="input-group well">
                <input type="text" class="form-control" id="search" name="search" placeholder="Buscar.." value="@Html.Raw(ViewBag.Search)" autocomplete="off" style="margin-bottom: 1px;">
                <span class="input-group-btn">
                    <button class="btn btn-primary" type="button">
                        <i class="clip-search-3"></i> Buscar
                    </button>
                </span>
            </div>
        </form>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <!-- start: BASIC TABLE PANEL -->
        <div class="panel panel-default">
            <div class="panel-heading">
                <i class="icon-external-link-sign"></i>
                Venta x Recibo
                <div class="panel-tools">
                    <a class="btn btn-xs btn-link panel-collapse collapses" href="javascript:void(0)">
                    </a>
                    <a class="btn btn-xs btn-link panel-expand" href="javascript:void(0)">
                        <i class="icon-resize-full"></i>
                    </a>
                    <a class="btn btn-xs btn-link panel-close" href="javascript:void(0)">
                        <i class="icon-remove"></i>
                    </a>
                </div>
            </div>
            <div class="panel-body">
                <table class="table table-hover">
                    <thead>
                        <tr>         
                            <th>Agencia</th>
                            <th>Recibo</th>
                            <th>Fecha</th>
                            <th>Hora</th>
                            <th>Cliente</th>
                            <th>Productos</th>
                            <th>Total</th>                           
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var Recibo in Model)
                        {
                            <tr> 
                                <td>@(Recibo.Agencia == null ? "No Disponible" : Recibo.Agencia.Nombre)</td>                               
                                <td>@Recibo.ReciboId</td>
                                <td>@Recibo.Fecha.ToString("dd/MM/yyyy")</td>                               
                                <td>@(Recibo.FechaHoraRecibo == null ? "00:00" : Recibo.FechaHoraRecibo.Value.ToString("hh:mm tt"))</td>                            
                                <td>@(Recibo.Cliente == null ? "No Disponible" : Recibo.Cliente.Nombre)</td>                            
                                <td>@(Recibo.Detalles == null ? 0 : (Recibo.Anulada ? 0 : Recibo.Detalles.Count()))</td>
                                <td>@(Recibo.Detalles == null ? "Q0.00" : (Recibo.Anulada ? "Q0.00" : Recibo.Detalles.Sum(x => x.Cantidad * x.Precio).ToString("C")))</td>                                
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- end: BASIC TABLE PANEL -->
    </div>
</div>

Pagina @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) de @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Venta_x_Recibo", new { search = ViewBag.Search, page }))

