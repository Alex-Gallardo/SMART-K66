@model PagedList.IPagedList<DiamDev.Give.Entities.UnidadConversion>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Unidad de Conversión Listado";
}

<div>
    <a class="btn btn-light-grey" href="@Url.Action("Crear", "Unidad_Conversion")">
        Nueva
        <i class="icon-circle-arrow-right"></i>
    </a>
</div>
<br />
<div class="row">
    <div class="col-md-12">
        <form action="" method="get" class="form-inline">
            <div class="input-group well">
                <input type="text" class="form-control" id="search" name="search" placeholder="Buscar.." value="@Html.Raw(ViewBag.Search)" autocomplete="off" style="margin-bottom: 1px;">
                <span class="input-group-btn">
                    <button class="btn btn-primary" type="button">
                        <i class="clip-search-3"></i> Buscar
                    </button>
                </span>
            </div>
        </form>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <!-- start: BASIC TABLE PANEL -->
        <div class="panel panel-default">
            <div class="panel-heading">
                <i class="icon-external-link-sign"></i>
                Unidad de Conversión
                <div class="panel-tools">
                    <a class="btn btn-xs btn-link panel-collapse collapses" href="javascript:void(0)">
                    </a>
                    <a class="btn btn-xs btn-link panel-expand" href="javascript:void(0)">
                        <i class="icon-resize-full"></i>
                    </a>
                    <a class="btn btn-xs btn-link panel-close" href="javascript:void(0)">
                        <i class="icon-remove"></i>
                    </a>
                </div>
            </div>
            <div class="panel-body">
                <table class="table table-hover" id="sample-table-1">
                    <thead>
                        <tr>
                            <th>Tipo de Operación</th>
                            <th>Unidad Base</th>
                            <th>Unidad Destino</th>                        
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var Unidad in Model)
                        {
                            <tr>
                                <td>@(Unidad.Operacion == null ? "No Disponible" : Unidad.Operacion.Nombre)</td>
                                <td>@(Unidad.UnidadBase == null ? "No Disponible" : Unidad.UnidadBase.Nombre)</td>
                                <td>@(Unidad.UnidadDestino == null ? "No Disponible" : Unidad.UnidadDestino.Nombre)</td>                    
                                <td class="center">
                                    <div class="visible-md visible-lg hidden-sm hidden-xs">
                                        <a href="@Url.Action("Editar", "Unidad_Conversion", new { id = Unidad.ConversionId })" class="btn btn-teal tooltips" data-placement="top" data-original-title="Editar"><i class="icon-edit"></i></a>                                        
                                    </div>
                                    <div class="visible-xs visible-sm hidden-md hidden-lg">
                                        <div class="btn-group">
                                            <a class="btn btn-primary dropdown-toggle btn-sm" data-toggle="dropdown" href="#">
                                                <i class="icon-cog"></i> <span class="caret"></span>
                                            </a>
                                            <ul role="menu" class="dropdown-menu pull-right">
                                                <li role="presentation">
                                                    <a role="menuitem" tabindex="-1" href="@Url.Action("Editar", "Unidad_Conversion", new { id = Unidad.ConversionId })">
                                                        <i class="icon-edit"></i> Editar
                                                    </a>
                                                </li>                                                
                                            </ul>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- end: BASIC TABLE PANEL -->
    </div>
</div>

Pagina @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) de @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index", new { search = ViewBag.Search, page }))

@section Scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            if ('@TempData["Unidad_Conversion-Success"]' == "OK")
            {
                toastr.success('La unidad de conversión se guardó exitosamente');
            }
        });
    </script>
}



